import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    icon: {
        pading: 0,
        margin: -5,
        fontSize: 40,
        color: 'Silver',
        opacity: '0.4',
    },
    root: {

    },
})


export default useStyles